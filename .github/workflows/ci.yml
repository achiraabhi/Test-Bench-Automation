name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-typecheck:
    name: Lint & type-check
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r instruments/requirements.txt
          pip install -r dashboard/requirements.txt
          pip install ruff mypy

      - name: Lint with ruff
        run: |
          ruff check instruments/visacom/ instruments/example.py instruments/example2.py instruments/diagnose.py
          ruff check dashboard/ui_server.py

      - name: Type-check with mypy
        run: mypy instruments/visacom/ --ignore-missing-imports
